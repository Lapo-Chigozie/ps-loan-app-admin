/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AppMonthlyNetPayDtService } from './AppMonthlyNetPayDt.service';

describe('Service: AppMonthlyNetPayDt', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AppMonthlyNetPayDtService]
    });
  });

  it('should ...', inject([AppMonthlyNetPayDtService], (service: AppMonthlyNetPayDtService) => {
    expect(service).toBeTruthy();
  }));
});
