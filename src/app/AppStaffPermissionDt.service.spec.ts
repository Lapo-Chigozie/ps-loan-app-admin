/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AppStaffPermissionDtService } from './AppStaffPermissionDt.service';

describe('Service: AppStaffPermissionDt', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AppStaffPermissionDtService]
    });
  });

  it('should ...', inject([AppStaffPermissionDtService], (service: AppStaffPermissionDtService) => {
    expect(service).toBeTruthy();
  }));
});
